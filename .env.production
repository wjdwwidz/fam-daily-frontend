# 운영 빌드(eas update · expo export)가 붙을 서버. 로컬 개발(expo start)은 이 파일을 읽지 않는다.
# 서버 주소는 앱 번들에 그대로 들어가는 공개 값이라 git 에 둔다 — 비밀 값은 절대 여기(EXPO_PUBLIC_*)에 넣지 말 것.
# APK 빌드는 eas.json, 웹(Vercel)은 vercel.json 에도 같은 주소가 있다. 바꿀 땐 세 곳을 함께.
EXPO_PUBLIC_API_URL=https://web-production-cb610.up.railway.app
